class Lightpath():
    def __init__(self, id, source, destination, links, slots, modulationLevel):
        self.id = id
        self.source = source
        self.destination = destination
        self.links = links
        self.slots = slots
        self.modulationLevel = modulationLevel